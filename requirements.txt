flask==2.3.3
python-dotenv==1.0.0
requests==2.31.0

ERROR: pip's dependency resolver does not currently take into account all the packages that are installed. This behaviour is the source of the following dependency conflicts.
pymilvus 2.5.5 requires python-dotenv<2.0.0,>=1.0.1, but you have python-dotenv 1.0.0 which is incompatible.
arxiv 2.1.3 requires requests~=2.32.0, but you have requests 2.31.0 which is incompatible.
datasets 3.3.2 requires requests>=2.32.2, but you have requests 2.31.0 which is incompatible.

pip3 install flask python-dotenv requests 